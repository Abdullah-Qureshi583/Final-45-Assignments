//This is intentional error, because we enter the type boolean instead of number
// let intentionalError : boolean[] = [1, 2,3, 4, 5];
// Now we coorect the error by change type to number
var intentionalErrors = [1, 2, 3, 4, 5];
console.log(intentionalErrors);
