// Assignment # 13

// Storing my favourite transport mode in an array.
let transportationMode : string[] = ["Honda 125", "Civic Car", "Bus", "Red Line Bus", "Green Line Bus"];

// Use for of loop to print them one by one with a message.
for (let mode of transportationMode){
    console.log(`I would like to own a ${mode}`)
}